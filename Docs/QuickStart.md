# 5-Minute Quick Start

## Option 1: Try SkyCMS Locally (Docker)

```bash
# No Azure/AWS account needed for local trial
docker run -d -p 8080:8080 toiyabe/sky-editor:latest
# Open http://localhost:8080

Option 2: Deploy to Azure (One Click)
[Deploy to Azure Button] → 3-minute setup wizard